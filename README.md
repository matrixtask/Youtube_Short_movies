# YouTube Shorts 量産パイプライン — 撮って投げるだけ

[X_Autopost](https://github.com/matrixtask/X_autopost) と同じインタビュー駆動の仕組みで、
**質問に答える動画を撮るだけ**でYouTubeショートが量産され、撮りためたショートは
1本のまとめ動画にもなるシステムです。

編集（変な間のカット・字幕・ツッコミ・挿絵・切り出し）は全部Claudeとffmpegがやります。

## 毎日のルーティン（あなたがやること）

1. **朝、Slackに撮影台本が届く**（質問5問＋ネタ指示。テーマは 定番 / 時事 / ネタ から自動選定）
2. **スマホを縦にして、質問に答える動画を撮る**（1本通しでOK。言い直し・変な間・噛みは気にしない）
3. **撮った動画を台本のスレッドにそのまま投稿する**（スマホの共有シートから2タップ）
4. 終わり。スマホで完結、PC不要。あとは全部クラウドで自動:
   - GASが動画を受け付けて編集キューに登録（台本と自動で紐づく）し、
     GitHub Actionsを即起動 → クラウドのランナーがダウンロード
   - 文字起こし（単語タイムスタンプ付き）
   - Claudeが編集プランを作成 → 1回答=1ショートで切り出し
   - 沈黙・言い直し・フィラーをカットしてテンポを作る
   - 字幕＋フックテキスト＋ツッコミ字幕を焼き込み、挿絵イラストを挿入
   - ショートごとに品質採点（品質ゲート）→ **合格したショートが台本のスレッドに
     動画ファイルで返ってくる**（そのままスマホから確認・投稿できる）
5. 溜まったら Slackに **「まとめて」** と書くだけ → クラウドが1本のまとめ動画
   （16:9・チャプター付き）を作ってSlackに投稿

## 全体像

```
 朝8時               随時（スマホだけ）                クラウド（GitHub Actions）
┌────────────┐  撮影 → スレッドに投稿  ┌──────────────┐   ┌────────────────────────┐
│ 撮影台本生成  │ ──────────────────▶ │ GASが受付      │ ─▶│ ダウンロード → 文字起こし   │
│ 質問+ネタ指示 │      （Slack）       │ キュー登録+起動  │   │ → 編集プラン → 間カット    │
└────────────┘                      └──────────────┘   │ → 字幕・挿絵 → 品質ゲート   │
       │                                               │ → ショート量産             │
       │             Slack「まとめて」                    └────────────────────────┘
       │                  │                              │ショートはスレッドに返る
       └──── スプレッドシート（台本・動画・ショート台帳） ──────┘
                          ▼
             まとめ動画をクラウドで生成 → Slackへ（16:9・チャプター付き）
```

- **撮影台本**: 質問ごとに「答え方のコツ」と「ネタ指示」（例: ここで一回真顔で沈黙）付き。
  スレッドへの返信は次の台本づくりのヒントとして記憶されます
- **変な間は全部カット**: 単語タイムスタンプから沈黙を検出して自動カット。
  言い直しやフィラー（えー、あのー）はClaudeが指定してカット
- **挿絵**: Claudeが描くフラットなSVGイラストをPNG化して話の山場に挿入
- **品質ゲート**: ショートごとに0-100点で採点し、閾値（既定70点）未満は
  レンダリングせずプランだけ残す。`plan.json` を直して再実行もできる

- **保存もSlack**: 生成したショートの実体はSlack上に置かれ、台帳はシートで管理。
  自前のサーバーもPCの常駐も不要（クラウド実行はGitHub Actionsの無料枠で動く）

## リポジトリ構成

```
gas/                撮影台本システム（Google Apps Script + Slack + スプレッドシート）
pipeline/           編集パイプライン（Python + faster-whisper + Claude + ffmpeg）
.github/workflows/  クラウド実行（動画処理 process-videos / まとめ動画 compile）
```

セットアップ手順:

- 台本がSlackに届くようにする → **[gas/README.md](gas/README.md)**
- 編集パイプライン（クラウド実行のSecrets設定 / ローカル実行）→
  **[pipeline/README.md](pipeline/README.md)**

クラウドなしでも動きます: PCで `ytshorts pull --watch` を常駐させるか、動画を
`workspace/inbox/` に入れて `ytshorts run` する手動モードも残してあります。
