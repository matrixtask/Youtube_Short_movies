# ytshorts の設定。`ytshorts init` でこのファイルが config.yaml にコピーされる。
# APIキーは環境変数で渡す: ANTHROPIC_API_KEY / GAS_ADMIN_TOKEN

# 作業ディレクトリ（inbox / sessions / shorts / compilations が作られる）
workspace: workspace

# 文字起こし（faster-whisper）
whisper_model: large-v3   # 遅ければ medium / small に
whisper_device: auto      # auto | cpu | cuda
language: ja

# 編集
claude_model: claude-sonnet-5
max_pause: 0.5            # これを超える沈黙（秒）はカット
pad: 0.15                 # 発話の前後に残す秒数
short_min_sec: 15
short_max_sec: 59
quality_threshold: 70     # このスコア未満のショートはレンダリングしない

# 見た目の自己採点（レンダリング後にテロップ・挿絵・サムネをvisionで採点）
visual_review: true       # false で無効化（API節約したいとき）
visual_threshold: 70      # この点未満はテロップ調整・挿絵の描き直しで再レンダリング
visual_max_retries: 2     # 作り直しの上限回数

# チャンネル・編集テイスト
channel_concept: "中井佑の、とにかく早く移動したい！"
style_notes: ""           # 編集テイストの指示（例: ツッコミは関西弁で辛口に、など）

# 出力
width: 1080
height: 1920
fps: 30
wide_enabled: true        # 横型ソースのとき16:9ワイド版も生成（まとめ・ロング用）
shorts_layout: fit        # 横型→ショート: fit=横幅フィット+上下帯 / crop=中央切り抜き
band_color: "#101820"     # fitレイアウトの帯の色

# 字幕スタイル
subtitle_font: "Noto Sans CJK JP"
caption_color: "#FFFFFF"  # 本文字幕
hook_color: "#FFE600"     # 冒頭フック
tsukkomi_color: "#FFD700" # ツッコミ・ネタ字幕
title_color: "#FFFFFF"    # 上帯の常時タイトル（fitのみ）

# レイアウト位置（画面に対する比率 0〜1）
text_side: center         # テロップの寄せ: center / left / right
                          # 話者が片側に立つ構図なら反対側（空きスペース側）を指定する
caption_margin: 0.177     # 本文字幕の下からの余白（大きいほど上に上がる）
tsukkomi_margin: 0.365    # ツッコミの上からの余白（大きいほど下に下がる）
caption_fontsize: 72      # 本文字幕の文字サイズ
video_y: 0.42             # fitレイアウトの映像の縦位置（0=上寄せ 0.5=中央）
illustration_width: 0.8   # 挿絵の幅（短辺に対する比率。縦横どちらでも同じ見た目になる）
illustration_x: 0.5       # 挿絵の中心の横位置（左から。0.27で左の空きスペースに置ける）
illustration_y: 0.38      # 挿絵の中心の縦位置（上から）

# GAS撮影台本システムとの連携（任意。空なら台本なしで動く）
gas_webapp_url: ""        # GASのWebアプリURL（gas/README.md参照）
