# 台本生成の分割実行と再開

Slackの「台本」と日次生成は、受付時にジョブを保存し、ワーカーが一段階ずつ実行する。
テーマ・問いの強度・3者会議・ミアの批評・質問数を維持し、通常の生成から全体240秒の制限を外した。
APIの応答を待つ間にSlackの受付処理を占有しない。

## 実行の流れ

| stage | 処理 | 保存する結果 |
|---|---|---|
| `themes` | テーマ選定（API 1回） | テーマ、直近30問、メモ、学習方針、採用文脈 |
| `discussion` | レイ・セバスチャン・ハンニバルの会議（API 1回） | 検証済みの会議結果 |
| `review` | ミアの批評（API 1回） | 検証済みの採否・6軸評価・修正指示 |
| `writing` | 台本執筆（API 1回） | 検証済みの質問、撮影カード、制作メモ |
| `parent` | Slack親投稿と台本・質問の保存 | 親のtsと保存済み行。保存中断時は`persist`から再開 |
| `delivery` | カードとメモの配信 | メッセージごとの送信位置 |

テーマのJSON形式不良は `themes_repair`、執筆の形式・品質不良は `writing_repair` として、
次の実行で1回だけ修復する。通常のモデル呼び出しは計4回、自動修復込み最大6回。
APIエラーや会議・批評の不合格は停止し、自動で何度も再生成しない。手動再開には追加のAPI呼び出しが生じ得る。

`runShootScriptJobs` の1分トリガーを初回受付時に自動登録する。`installTriggers()` にも追加済み。
待機を含めて完成まで数分以上かかる。正確な起動時刻・実APIの生成所要時間は実環境に依存する。
登録権限の問題が出る場合はGASエディタで `installTriggers()` を実行して必要な権限を認可する。

## 状態の確認と再開

- `台本 状態`: 直近5ジョブのID・段階・状態を確認。
- `台本 再開 <ジョブID>`: `failed` のジョブを保存位置から再開。会議・批評など保存済みの段階は繰り返さない。
- `queued` は次回実行待ち、`running` は実行中、`done` は全配信完了。
- `needs_review` はSlack送信の成否が不明。重複投稿防止のため、通常の再開コマンドでは再送しない。

同時に届いた追加台本の依頼は、生成中の追加ジョブにまとめる。日次生成は同じ日付のジョブを重複作成しない。
失敗後に新しい「台本」を依頼すると別の追加ジョブになる。途中結果を使う場合は「台本 再開」を使う。
旧版の240秒エラーには保存済みジョブがないため、この変更で当時の途中結果を復元することはできない。

## Logと停止検知

`script_stage_start` / `script_stage_done` / `script_stage_failed` / `script_stage_interrupted` に
`job_id`・`stage`・`attempt`・`elapsed_ms`・`status`・`next_stage` を残す。
`elapsed_ms` はその段階の開始からの経過時間。APIの待ち時間だけでなく検証・保存を含む。
JSON不良の生のモデル出力やHTTPエラー本文はジョブ・失敗通知へ書かない。

[GASの1実行上限は6分](https://developers.google.com/apps-script/guides/services/quotas)。
1回のAPI自体が上限を超える問題は残る。実行中のジョブを8分経過後にワーカーが停止扱いにし、通知する。
この8分は実行権の失効を判定するための猶予で、GASの実行上限を延長するものではない。
[時間主導トリガー](https://developers.google.com/apps-script/guides/triggers/installable)の遅延・停止や、日次実行時間の割当超過時には、検知・続行も遅れる。

## 保存と重複配信への対処

`ScriptJobs` シートを自動追加する。既存のScripts/Questions列や外部API形式は変更しない。
JSONは1行の8セルへ分割し、段階と結果を1回の行更新で保存する。1ジョブのJSON上限は約24万UTF-16文字。
絵文字の途中では分割せず、セルには `json:` を付けて数式として扱われないようにする。
完了後は大きな入力を除き、質問本文はQuestions、会議・批評はLog、完了状態はScriptJobsに残す。
未完了・失敗時の入力と結果は再開のため残す。

ワーカーは実行権と短時間ロックで直列化する。長いAPI待ちの間はロックを解放する。
台本行はscript_id、質問行はscript_id/idxを確認して保存し、保存途中から再開しても二重追加しない。
Slackには各送信前に送信予定位置を保存し、ACK後に次の位置を保存する。投稿間隔は1秒以上。
Slack送信とSheetsの更新を単一トランザクションにできないため、成否不明時は `needs_review` で停止する。

管理者は `readShootJobs()` で対象ジョブの `data.sending` を確認し、Slackと照合する。
`-1` は親投稿、`0` 以降は `data.messages` の位置。確認後、GASエディタの一時関数から次を呼べる。

```javascript
// 実際のジョブIDと、照合したメッセージのtsを渡す。トークンの実値を書き込まない。
resolveShootScriptDelivery('対象のジョブID', '確認済みのSlack ts', requireProp('ADMIN_TOKEN'));
// 投稿されていないと確認できた場合だけ、第2引数を 'not_sent' にして再送を許可する。
```

不明なまま `not_sent` を使わない。解決操作はLogに残り、次のワーカー実行で続行する。
台本ワーカー以外の通知・他のボットとの送信競合は制御対象外。

## 反映と検証

運用機で `git pull --ff-only && make gas-deploy`。既存デプロイURL・秘密情報・本番依存は変更不要。
その後「台本」を依頼し、ScriptJobsの進行とLogの段階別時間を確認する。
GAS 85件・Python 154件のテストに成功。合計320秒の模擬生成、実行環境を作り直した再開、
重複tick、形式修復、停止検知、途中の行保存失敗、送信直後・完了時の保存失敗をスタブで検証した。
実GASのトリガー発火・権限・実API・Slackへの配信は未検証。
